"""Module Content — bài viết (Thủ thuật/Tin tức) + FAQ hiển thị trên landing.

Nội dung biên tập org-scoped, chỉ đọc công khai qua module `public`
(xem `app.modules.public`). Không chứa giá vốn/biên lợi nhuận — an toàn để lộ.
"""
